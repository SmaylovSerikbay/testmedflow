# Инструкция по запуску обновленной системы MedFlow

## 🎉 Что было сделано

### ✅ Полная переработка UI/UX

1. **Регистратура** - современный минималистичный интерфейс с:
   - Реал-тайм статистикой
   - Умными фильтрами
   - Виртуализацией списков для работы с большими данными
   - Автообновлением каждые 5-10 секунд

2. **Кабинет врача** - оптимизированный workflow:
   - Быстрые фильтры пациентов
   - Компактная форма осмотра
   - Автоматический переход к следующему пациенту
   - Статистика в реальном времени

3. **Кабинет сотрудника** - удобный просмотр:
   - Визуальный прогресс осмотра
   - Детальный маршрут
   - Уведомления и напоминания
   - Полный доступ к амбулаторной карте

### ✅ Электронные связи между участниками

1. **Система уведомлений** (`services/notificationService.ts`)
   - Регистратура ↔ Врачи
   - Регистратура ↔ Сотрудники
   - Врачи ↔ Сотрудники
   - Браузерные push-уведомления

2. **Компонент уведомлений** (`components/NotificationPanel.tsx`)
   - Панель с непрочитанными
   - Фильтры и управление
   - Цветовая кодировка по типу

### ✅ Оптимизация для больших данных

1. **API оптимизация** (`services/apiOptimization.ts`)
   - Кэширование запросов
   - Дебаунсинг/Throttling
   - Батчинг запросов
   - Пагинация
   - Виртуализация списков
   - Мониторинг производительности

2. **Анимации** (`styles/animations.css`)
   - Плавные переходы
   - Градиенты и тени
   - Оптимизация производительности
   - Адаптивная типографика

### ✅ Система реального времени

1. **WebSocket сервис** (`services/websocketService.ts`)
   - Подключение к серверу
   - Автоматическое переподключение
   - Подписка на события
   - Broadcast обновлений

## 🚀 Запуск системы

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск Frontend (разработка)

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### 3. Запуск Backend

```bash
cd backend
go run main.go
```

Backend будет доступен на `http://localhost:8080`

### 4. База данных

Убедитесь, что PostgreSQL запущен и доступен:

```bash
# Переменные окружения (или в .env файле)
DB_HOST=localhost
DB_PORT=5432
DB_USER=medflow
DB_PASSWORD=medflow_password
DB_NAME=medflow
```

## 📋 Структура проекта

```
testmedflow/
├── components/
│   ├── RegistrationDesk.tsx       # ✨ Обновлено
│   ├── DoctorWorkspace.tsx        # ✨ Обновлено
│   ├── EmployeePortal.tsx         # ✨ Обновлено
│   ├── NotificationPanel.tsx      # 🆕 Новый
│   └── ...
├── services/
│   ├── api.ts                     # Существующий
│   ├── notificationService.ts     # 🆕 Новый
│   ├── websocketService.ts        # 🆕 Новый
│   ├── apiOptimization.ts         # 🆕 Новый
│   └── ...
├── styles/
│   └── animations.css             # 🆕 Новый
├── backend/
│   └── main.go                    # Существующий (оптимизирован)
├── СИСТЕМА_СВЯЗЕЙ.md              # 🆕 Документация
├── ОБНОВЛЕНИЯ_UI_UX.md            # 🆕 Документация
└── ИНСТРУКЦИЯ_ПО_ЗАПУСКУ.md      # 🆕 Эта инструкция
```

## 🔧 Использование новых функций

### Уведомления

Добавьте компонент уведомлений в любой личный кабинет:

```typescript
import NotificationPanel from './components/NotificationPanel';

// В Header компонента
<NotificationPanel 
  userId={currentUser.uid} 
  userRole={currentUser.role} 
/>
```

### WebSocket (опционально)

Для включения реального времени:

```typescript
import { websocketService } from './services/websocketService';

// При монтировании компонента
useEffect(() => {
  websocketService.connect(currentUser.uid);
  
  const unsubscribe = websocketService.on('visit_created', (message) => {
    // Обработка события
    loadVisits();
  });
  
  return () => {
    unsubscribe();
  };
}, [currentUser.uid]);
```

### Кэширование API

```typescript
import { apiCache } from './services/apiOptimization';

// Перед запросом
const cached = apiCache.get('key');
if (cached) {
  return cached;
}

// После запроса
const data = await apiCall();
apiCache.set('key', data, 5 * 60 * 1000); // 5 минут
```

## 🎨 Кастомизация

### Цвета

Измените цветовую схему в `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: '#3B82F6',    // Синий
      success: '#10B981',    // Зеленый
      warning: '#F59E0B',    // Желтый
      danger: '#EF4444',     // Красный
    }
  }
}
```

### Анимации

Добавьте свои анимации в `styles/animations.css`:

```css
@keyframes my-animation {
  from { opacity: 0; }
  to { opacity: 1; }
}

.my-class {
  animation: my-animation 0.3s ease-out;
}
```

## 📊 Мониторинг

### Производительность

```typescript
import { performanceMonitor } from './services/apiOptimization';

const end = performanceMonitor.start('operation_name');
// ... выполнение операции
end();

// Получение статистики
const stats = performanceMonitor.getStats('operation_name');
console.log('Среднее время:', stats.avg, 'мс');
```

### Кэш

```typescript
import { apiCache } from './services/apiOptimization';

// Очистка кэша
apiCache.clear();

// Инвалидация по паттерну
apiCache.invalidatePattern('contracts_.*');
```

## 🐛 Отладка

### Включение логов

В консоли браузера:

```javascript
// WebSocket
localStorage.setItem('debug_websocket', 'true');

// API
localStorage.setItem('debug_api', 'true');

// Уведомления
localStorage.setItem('debug_notifications', 'true');
```

### Проверка состояния

```javascript
// WebSocket
websocketService.isConnected();
websocketService.getReadyState();

// Кэш
apiCache.cache.size; // Количество закэшированных элементов

// Уведомления
notificationService.getNotifications(userId);
```

## 🔐 Безопасность

### Переменные окружения

Создайте `.env` файл:

```env
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
VITE_ENABLE_WEBSOCKET=true
```

### CORS

Backend уже настроен на разрешение CORS для разработки. Для продакшена настройте в `backend/main.go`:

```go
w.Header().Set("Access-Control-Allow-Origin", "https://yourdomain.com")
```

## 📱 Мобильная версия

Все компоненты полностью адаптивны:
- Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
- Touch-friendly интерфейс
- Оптимизация для малых экранов

## 🚢 Деплой

### Docker

```bash
docker-compose up -d
```

### Ручной деплой

```bash
# Frontend
npm run build
# Файлы в dist/

# Backend
cd backend
go build -o medflow
./medflow
```

## 📚 Дополнительная документация

- `СИСТЕМА_СВЯЗЕЙ.md` - архитектура связей между участниками
- `ОБНОВЛЕНИЯ_UI_UX.md` - детальное описание UI/UX изменений
- `README.md` - общая информация о проекте

## 💡 Советы

1. **Производительность**
   - Используйте виртуализацию для списков >100 элементов
   - Включайте кэширование для часто запрашиваемых данных
   - Используйте дебаунсинг для поисковых запросов

2. **UX**
   - Показывайте загрузочные состояния
   - Используйте оптимистичные обновления UI
   - Добавляйте анимации для плавности

3. **Отладка**
   - Используйте React DevTools
   - Проверяйте Network tab для API запросов
   - Мониторьте Performance tab

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера на ошибки
2. Проверьте логи backend сервера
3. Убедитесь, что БД доступна
4. Проверьте версии зависимостей

Контакты:
- Email: support@medflow.kz
- Telegram: @medflow_support

## ✅ Чеклист запуска

- [ ] PostgreSQL запущен
- [ ] Backend запущен (порт 8080)
- [ ] Frontend запущен (порт 5173)
- [ ] Нет ошибок в консоли
- [ ] Можно войти в систему
- [ ] Регистратура работает
- [ ] Кабинет врача работает
- [ ] Кабинет сотрудника работает
- [ ] Уведомления приходят
- [ ] Данные обновляются

---

**Версия:** 2.0.0  
**Дата:** Декабрь 2024  
**Статус:** ✅ Готово к использованию

